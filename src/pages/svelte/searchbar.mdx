---
title: 'Searchbar Svelte Component | Konsta UI Svelte'
section: 'svelte'
---

import { SearchbarProps } from '@/components/api/svelte/SearchbarProps';
import SearchbarExample from '@/components/examples/svelte/Searchbar';

# Searchbar Svelte Component

Searchbar allows user to search through [List View](/svelte/list) elements. Or it can be used as a visual UI component for your custom search realization.

## Searchbar Components

There are following components included:

- `Searchbar` 

## Searchbar Props

<SearchbarProps />

## Examples

<SearchbarExample />
