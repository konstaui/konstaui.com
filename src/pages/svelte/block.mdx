---
title: 'Block Svelte Component | Konsta UI Svelte'
section: 'svelte'
---

import { BlockProps } from '@/components/api/svelte/BlockProps';
import { BlockTitleProps } from '@/components/api/svelte/BlockTitleProps';
import { BlockHeaderProps } from '@/components/api/svelte/BlockHeaderProps';
import { BlockFooterProps } from '@/components/api/svelte/BlockFooterProps';
import BlockExample from '@/components/examples/svelte/ContentBlock';

# Block Svelte Component

Block Svelte component represents Content Block element designed (mostly) to add extra formatting and required spacing for text content.

## Block Components

There are following components included:

- `Block` - main block element
- `BlockTitle` - block title. Can be used before Block or List View
- `BlockHeader` - extra header element. Can be used both inside and outside of block elements
- `BlockFooter` - extra footer element. Can be used both inside and outside of block elements

## Block Props

<BlockProps />

## BlockTitle Props

<BlockTitleProps />

## BlockHeader Props

<BlockHeaderProps />

## BlockFooter Props

<BlockFooterProps />

## Examples

<BlockExample />
